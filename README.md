## TODOs:


### Cross-Compatibility
1. environment files
2. Allow only a certain size of cube to be loaded based on system RAM & GPU VRAM OR just load the max permissable portion of the cube at a time

### Core Functionality
1. Support to load cubes files so that the UI does not need to be re-run (handle memory when opening/flushing & gc collection)

### Optimizations
1. Parallelize/Optimize averaged scrubbing. Too slow right now
 